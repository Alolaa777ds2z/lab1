Console.WriteLine("Esciba un numero1: ");
int numero1 = int.Parse(Console.ReadLine());

if (numero1 <0)
{
    Console.WriteLine("no eres bienvenido");

} 

else
{
    while (numero1 > 0)
    {
        Console.WriteLine("bienvenido");
        break;
    }
    
}